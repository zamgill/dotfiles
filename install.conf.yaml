- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#    ~/.vim: vim
#    ~/.vimrc: vimrc
     ~/.zshrc:
     ~/.gitconfig:

- create:
    - ~/Projects

# - shell:
#  - [git submodule update --init --recursive, Installing submodules]
